import 'package:flutter/material.dart';
class SellerHomescreen extends StatelessWidget {
  const SellerHomescreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("seller homescreen"),
      ),
    );
  }
}
